(function(){const $=MathSphere.$;let bank;fetch("data/question-banks.json").then(r=>r.json()).then(x=>{bank=x;render()});function render(){const b=$("#guessBoard")?.value,c=$("#guessClass")?.value,o=$("#guessContent");if(!o)return;if(!b||!c){o.innerHTML='<div class="empty-state">Select board and class.</div>';return}const qs=bank[b][c].mcqs.slice(0,25);o.innerHTML=`<article class="generated-paper"><span class="unofficial-badge">UNOFFICIAL GUESS PAPER</span><h3>${b.toUpperCase()} Class ${c} Mathematics</h3><p class="muted">High-priority original practice questions selected from curriculum topics. Not a prediction or official paper.</p>${qs.map((q,i)=>`<div class="paper-section"><strong>${i+1}. ${MathSphere.escape(q.q)}</strong><div class="mcq-options">${q.o.map((x,j)=>`<span>${String.fromCharCode(65+j)}. ${MathSphere.escape(x)}</span>`).join("")}</div></div>`).join("")}</article>`}$("#guessBoard")?.addEventListener("change",render);$("#guessClass")?.addEventListener("change",render)})();